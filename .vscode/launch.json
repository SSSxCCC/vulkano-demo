{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(Windows) Launch",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceRoot}/target/debug/minimal.exe",
            "cwd": "${workspaceRoot}",
            "console": "integratedTerminal",
            "environment": [{ "name": "RUST_BACKTRACE", "value": "full" }],
            "preLaunchTask": "cargo build"
        }
    ]
}